// Declare a variable and initialize it to null
let myVariable = null;

// Use the nullish operator to set a default value
let myValue = myVariable ?? "default value";

console.log(myValue); // Output: "default value"
